# TCP MEMORY

**Agent:** TCP
**Reports to:** H1 (peer of Plato)
**Updated:** 2026-09-01T13:40:00-0500

---

## Stable TCP-local facts

- **Canonical TCP already exists in corpus** (not greenfield):
  - `D:\MW_CENTRAL\drayl-t2\Turing Checkpoint (TCP) and the Turing Checkpoint Checker (TCPC) .docx` (Dec 18 2025)
  - `D:\MW_CENTRAL\drayl-t2\_The Architecture of the Anchor (TCP).docx` (Jan 30 2026)
  - Canonical invariant: `active_apps/anancy-io-main/mindwave_invariants/mw_16_tcp_tcpc.md`
- **Chrono-Drayl is the existing canonical name for the timeline** — `mw_112_chrono_drayl.md` ("Time is the OS of meaning").
- **"Suitable" is a defined governance term** — `mw_10_suitability_vs_correctness.md` (go/no-go readiness, NOT "correctness").
- **Generational Drayl Inheritance** — `mw_111_generational_drayl_inheritance.md` ("your son won't inherit notes; he'll inherit you").
- **Existing (drifted) implementation** — `active_apps/anancy-io-main/python/helpers/tcp_tcpc/` — `tcpc.py` imports `CheckpointType`/`CheckpointChain` that don't exist in `tcp.py`; `examples.py` references methods absent from `tcpc.py`. Implementation is a shell; spec is rich.

## Position in the mesh

- **TCP is a peer of Plato** (both report to H1), not a child of Plato. This is required so TCP can view Plato's governance actions and record them in the Chrono-Drayl. Onu reports to Plato; TCP does not.

## Blocked state

Model selection is being researched in a separate chat. TCP must **not scan drayl/obsidian** until that resolves. Scaffold only.
