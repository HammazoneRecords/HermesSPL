agent:
  name: TCP
  id: TCP
  reports_to: H1
  peer_of: Plato
  role: chrono_drayl_formalizer
  name_meaning: turing_checkpoint_agent
mode:
  default: scaffold_only
  writes_require_scope: true
  destructive_actions: forbidden_without_explicit_owner_approval
  scan_drayl_obsidian: blocked_until_model_selection
  view_plato_actions: true   # peer right — read governance surfaces
  write_outside_agent_space: false
model_preferences:
  # PENDING — model selection is being researched in a separate chat.
  formalization_default: TBD
  simple_task_handoff: TBD
  fallback: TBD
