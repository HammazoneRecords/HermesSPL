# AppCtx USER

User prefers exact paths, verified app facts, and no destructive cleanup. User wants agents to understand Jamaican/Creole patois directly and preserve voice when documenting app/domain language.

For app work, user wants every app to have a markdown context document containing variables, syntax, commands, routes, env names, deploy paths, current priority, and progress notes.
